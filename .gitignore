# Terraform working files
.terraform/
# Note: .terraform.lock.hcl IS committed (locks provider versions for CI).

# Local state (used by _platform for one-shot bootstrap)
*.tfstate
*.tfstate.*
*.tfstate.backup

# Saved plans
*.tfplan
tfplan
plan.txt

# Crash logs
crash.log
crash.*.log

# Override files
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Editor / OS
.DS_Store
.vscode/
.idea/
